#include <iostream>
using namespace std;

int main() {
    int n;
    cin>>n;
    char ar[n];
    count=0;
    for(int i=0;i<n;i++)
    {
        cin>>ar[i];
    }
    for(i=0;i<n;i++)
    {
        if(a[i]=='U' && a[n-i])=='D')
        
        count++;
        
    }
    
    cout<<count;

    return 0;
}
